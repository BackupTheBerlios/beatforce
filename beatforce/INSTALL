use
python scons.py

to build beatforce with the default user interface.(sdltk)

use 
python scons.py ui=gtk

or
python scons.py ui=qt

to build either gtk or a qt interface

as root run

pyton scons.py install

to install all files